{
    "name": "answers-as-a-service",
    "type": "module",
    "scripts": {
        "dev": "bun wrangler dev",
        "deploy": "wrangler deploy --minify",
        "cf-typegen": "wrangler types --env-interface CloudflareBindings",
        "test": "bun test"
    },
    "dependencies": {
        "@hono/standard-validator": "^0.2.2",
        "hono": "^4.12.12",
        "valibot": "^1.3.1"
    },
    "devDependencies": {
        "@types/bun": "^1.3.12",
        "wrangler": "^4.83.0"
    }
}
